---
title: datapin
---

# Pin, sync, and publish research data

`datapin` is a single-binary CLI that publishes your data to a FAIR archive
with a DOI — and, before it is ready for that, keeps it moving between the
cluster and your laptop. One committed manifest (`.datapin/datapin.toml`) pins
every file to an exact version and checksum, so a `git clone` is enough to get
the bytes back.

```console
$ datapin publish counts      # dataset → Zenodo/Dataverse/Figshare record + DOI + citation
$ datapin check --fair        # DataCite/SPDX/ORCID lint + F-UJI FAIR score
$ datapin cite counts         # paste-ready citation for the minted DOI
$ datapin push counts         # same files → workspace remote (versioned, no DOI)
```

Install it and go:

```bash
curl -fsSL https://raw.githubusercontent.com/BU-Neuromics/datapin/main/install.sh | bash
```

This site is itself generated by `datapin site publish` from the manifest in
the datapin repository — the same command that builds a landing page for your
datasets built these pages.

## Start here

If you have a results folder and a deadline, read one guide:

- **[Your first DOI](first-doi/)** — from a folder of files to a resolving
  DOI and a citation, rehearsed on a sandbox first.

## Guides

Task-shaped walkthroughs. Each one is a single sitting.

| Guide | When you want it |
|---|---|
| [Your first DOI](first-doi/) | You have finished data and need something citable. |
| [Publishing a new version](new-version/) | The data changed after you published it. |
| [Cluster to laptop](workspace/) | Results that need to travel and keep history, but not a DOI. |
| [Leaving OSF](leaving-osf/) | OSF is sunsetting its projects service and you need out. |
| [Institutional repositories](institutional/) | Your target is your university's InvenioRDM or Dataverse, not Zenodo. |
| [Scripting and CI](automation/) | Publishing from a pipeline, with no terminal to prompt. |

## When something goes wrong

- **[Troubleshooting](troubleshooting/)** — every error datapin raises on
  purpose, what it means, and the one command that resolves it.

## Reference

- [Command reference](https://github.com/BU-Neuromics/datapin#readme) — every
  command, flag, and manifest field, kept in the repository README so it cannot
  drift from the code.
- [Agent skill](https://github.com/BU-Neuromics/datapin/blob/main/skills/datapin/SKILL.md)
  — install with `npx skills add BU-Neuromics/datapin` to teach an AI coding
  agent the whole CLI.
- [Changelog](https://github.com/BU-Neuromics/datapin/blob/main/CHANGELOG.md)
  and [roadmap](https://github.com/BU-Neuromics/datapin/blob/main/ROADMAP.md).

## Two kinds of remote, one manifest

|  | **Archive remote** | **Workspace remote** |
|---|---|---|
| For | finished, citable data | intermediate results in flight |
| Kinds | `invenio` (Zenodo + any InvenioRDM), `dataverse`, `figshare` | `dir` (mounted NAS), `s3` (MinIO/R2/institutional), `sftp` (any cluster) |
| Identity | a DOI, resolving forever | a name; no DOI, no metadata ceremony |
| Mutability | published versions are immutable | overwrite freely; superseded bytes are archived |
| Verb | `datapin publish <slug>` | `datapin push <slug>` |
| History | version chain + concept DOI | append-only journal, `revert` to any version |

## Citing datapin

If datapin helped produce a dataset you published, cite it — GitHub renders a
**Cite this repository** button from
[`CITATION.cff`](https://github.com/BU-Neuromics/datapin/blob/main/CITATION.cff).
