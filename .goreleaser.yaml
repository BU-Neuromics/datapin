version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: datapin
    main: .
    binary: datapin
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/BU-Neuromics/datapin/cmd.version={{.Version}}

archives:
  - id: datapin
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"

release:
  github:
    owner: BU-Neuromics
    name: datapin

changelog:
  disable: true # release notes are sourced from CHANGELOG.md by the workflow
